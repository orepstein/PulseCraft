datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model Event {
  id        Int      @id @default(autoincrement())
  userId    String
  eventType String
  payload   Json
  createdAt DateTime @default(now())
}
